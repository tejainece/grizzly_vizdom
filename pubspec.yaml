name: vizzie_dom
description: Fluent data driven documents based selection and data-join API for HTML DOM
version: 0.0.2-dev
homepage: http://grizzly-dart.github.io
author: Ravi Teja Gudapati <tejainece@gmail.com>
documentation:

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:

dev_dependencies:
  browser: ^0.10.0
  dart_to_js_script_rewriter: ^1.0.1

transformers:
- dart_to_js_script_rewriter

web:
  compiler:
    debug: dartdevc
